export enum SessionType {
  individual = 'Individual',
  group = 'Group',
}

export enum SessionStatus {
  cancelled = 'Cancelled',
  completed = 'Completed',
  missed = 'Missed',
  upcoming = 'Upcoming',
}

export enum NoteStatus {
  pending = 'Pending',
  completed = 'Completed',
}
